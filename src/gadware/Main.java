package gadware;

import java.util.Arrays;
import java.util.List;

public class Main {


    public static void main (String[] args) {

        List<String> fruits = Arrays.asList("apple", "fig", "banana", "orange", "grabs");

        //fruits.stream().map(fruit -> fruit.toUpperCase(Locale.ROOT)).reduce("", fruit)

        // int[] arrSorted = Arrays.stream(arr).sorted().toArray();

    }




}
